function addTwoTogether() {
    return 1+2
}

// its synatxially correct but it will always give 3 : limited in capabilties but if i did this it can be useful

function addTwoTogether(a,b) {
    return a+b
}